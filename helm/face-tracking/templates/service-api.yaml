apiVersion: v1
kind: Service
metadata:
  name: {{ include "face-tracking.fullname" . }}-api
  labels:
    app: {{ include "face-tracking.fullname" . }}-api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.api.port }}
      targetPort: {{ .Values.service.api.port }}
  selector:
    app: {{ include "face-tracking.fullname" . }}-api
